using MultiProperties;

Console.ReadLine();

var list = new List<RedundantPropertiesObject>();

for (int i = 0; i < 1; i++)
{
    var obj = new RedundantPropertiesObject();
    list.Add(obj);
}

Console.ReadLine();